// constants/GlobalStyles.js 또는 styles/GlobalStyles.js
import { StyleSheet } from 'react-native';

// 앱 전체에서 재사용할 수 있는 상수 및 색상 정의 (공간 복잡도 최소화)
const PRIMARY_COLOR = '#646cff'; // 링크, 버튼 호버 색상
const DANGER_COLOR = '#535bf2'; // 링크 호버 색상
const DEFAULT_TEXT_COLOR = 'rgba(255, 255, 255, 0.87)';
const DEFAULT_BG_COLOR = '#242424';
const TOPBAR_HEIGHT = 64;

/**
 * GlobalStyles
 * @description 앱의 전역 및 재사용 가능한 스타일을 모듈화합니다.
 * 시간 복잡도: O(1) - 스타일 정의는 런타임에 한 번만 처리됩니다.
 */
export const GlobalStyles = StyleSheet.create({

    // 1. 앱 전역 컨테이너 스타일 (기존 #root, body)
    // App.jsx의 최상위 <View>에 적용되어야 합니다.
    container: {
        flex: 1, // 화면 전체를 차지
        backgroundColor: DEFAULT_BG_COLOR,
        minWidth: 320,
    },

    // 2. 기본 텍스트 및 링크 스타일 (기존 :root, a)
    baseText: {
        fontFamily: 'system-ui',
        lineHeight: 22,
        fontWeight: '400',
        color: DEFAULT_TEXT_COLOR,
    },
    link: {
        fontWeight: '500',
        color: PRIMARY_COLOR,
        textDecorationLine: 'none',
    },
    // 참고: :hover 스타일은 Pressable 컴포넌트의 state로 처리되거나,
    // TouchableOpacity의 activeOpacity로 대체됩니다.

    // 3. 컴포넌트 재사용 스타일
    card: {
        padding: 16,
        backgroundColor: '#333333',
        borderRadius: 8,
    },
    readTheDocs: {
        color: '#888888',
    },

    // 4. 버튼 스타일 (TouchableOpacity 또는 Pressable에 적용)
    button: {
        borderRadius: 8,
        borderWidth: 1,
        borderColor: 'transparent',
        paddingVertical: 10,
        paddingHorizontal: 20,
        backgroundColor: '#1a1a1a',
        alignItems: 'center', // 텍스트 중앙 정렬
        justifyContent: 'center',
    },
    buttonText: {
        fontSize: 16,
        fontWeight: '500',
        fontFamily: 'inherit',
        color: DEFAULT_TEXT_COLOR,
    },
    // 버튼 호버/포커스 효과를 위한 스타일 (Pressable의 state에서 사용할 수 있음)
    buttonHover: {
        borderColor: PRIMARY_COLOR,
    },
});

/**
 * TopbarStyles
 * @description Topbar 컴포넌트 전용 스타일
 * Topbar.jsx에서 import 하여 사용합니다.
 */
export const TopbarStyles = StyleSheet.create({
    topbar: {
        width: '100%',
        height: TOPBAR_HEIGHT,
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        paddingHorizontal: 15,
        backgroundColor: '#FFFFFF',
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.1,
        shadowRadius: 3,
        elevation: 4, // Android 그림자
        zIndex: 10,
    },
    left: {
        alignItems: 'center',
        justifyContent: 'center',
        paddingRight: 15,
    },
    logo: {
        fontSize: 20,
        fontWeight: 'bold',
        color: PRIMARY_COLOR,
    },
    right: {
        flexDirection: 'row',
    },
    btn: {
        paddingVertical: 8,
        paddingHorizontal: 12,
        marginLeft: 10,
        borderRadius: 5,
        backgroundColor: '#F0F0F0',
    },
    btnActive: {
        backgroundColor: PRIMARY_COLOR,
    },
    btnText: {
        color: '#333333',
        fontWeight: '500',
    },
    btnTextActive: {
        color: '#FFFFFF',
    },
});